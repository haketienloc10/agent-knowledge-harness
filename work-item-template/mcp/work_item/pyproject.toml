[project]
name = "global-work-item-mcp"
version = "0.1.0"
description = "Global mutable product Work Item MCP"
requires-python = ">=3.10"
dependencies = [
  "mcp==2.1.1",
  "pydantic>=2,<3",
]

[tool.uv]
package = false
