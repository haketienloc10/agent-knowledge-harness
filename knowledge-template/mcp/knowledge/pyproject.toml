[project]
name = "shared-knowledge-mcp"
version = "0.1.0"
description = "Repository-independent Markdown knowledge MCP"
requires-python = ">=3.10"
dependencies = [
  "filelock>=3,<4",
  "mcp>=2,<3",
  "pydantic>=2,<3",
  "PyYAML>=6,<7",
]

[tool.uv]
package = false
