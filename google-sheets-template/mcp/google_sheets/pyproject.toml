[project]
name = "google-sheets-readonly-mcp"
version = "0.1.0"
description = "Allowlisted read-only Google Sheets MCP for Codex CLI"
requires-python = ">=3.10"
dependencies = [
  "google-auth>=2,<3",
  "mcp==2.1.1",
  "requests>=2,<3",
]

[tool.uv]
package = false
